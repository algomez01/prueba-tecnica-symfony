<form method="post" action="{{ path('app_medicos_delete', {'id': medico.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ medico.id) }}">
    <button class="btn btn-danger"</button>Borrar</button>
</form>
