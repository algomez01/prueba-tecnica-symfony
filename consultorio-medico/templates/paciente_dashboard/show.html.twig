{% extends 'base.html.twig' %}

{% block title %}Citas{% endblock %}

{% block body %}
    <h1>Citas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cita.id }}</td>
            </tr>
            <tr>
                <th>Fe_creacion</th>
                <td>{{ cita.feCreacion ? cita.feCreacion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Motivo</th>
                <td>{{ cita.motivo }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ cita.estado }}</td>
            </tr>
            <tr>
                <th>TipoCitaId</th>
                <td>{{ cita.tipoCitaId }}</td>
            </tr>
            <tr>
                <th>Medico</th>
                <td>{{ cita.medicoId }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_paciente_dashboard_index') }}">back to list</a>

    <a href="{{ path('app_paciente_dashboard_edit', {'id': cita.id}) }}">edit</a>

    {{ include('paciente_dashboard/_delete_form.html.twig') }}
{% endblock %}
