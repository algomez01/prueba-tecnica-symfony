{% extends 'base.html.twig' %}

{% block title %}Comentario{% endblock %}

{% block body %}
    <h1>Comentario</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comentario.id }}</td>
            </tr>
            <tr>
                <th>Comentario</th>
                <td>{{ comentario.comentario }}</td>
            </tr>
            <tr>
                <th>Userid</th>
                <td>{{ comentario.userid }}</td>
            </tr>
            <tr>
                <th>Publicacionid</th>
                <td>{{ comentario.publicacionid }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_comentario_index') }}">back to list</a>

    <a href="{{ path('app_comentario_edit', {'id': comentario.id}) }}">edit</a>

    {{ include('comentario/_delete_form.html.twig') }}
{% endblock %}
