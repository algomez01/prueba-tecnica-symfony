{% extends 'base.html.twig' %}

{% block title %}Listado de usuarios{% endblock %}

{% block body %}
    <h2>Lista de usuarios</h2>

    <table class="table">
        <tbody>
            <tr>
                <th>Nombre</th>
                <td>{{verRepository.nombre}}</td>
            </tr>
        </tbody>
    </table>

    {# <a href="{{ path('app_publicaciones_index') }}">back to list</a>

    <a href="{{ path('app_publicaciones_edit', {'id': userRepository.id}) }}">editar</a>

    {{ include('publicaciones/_delete_form.html.twig') }} #}
{% endblock %}
