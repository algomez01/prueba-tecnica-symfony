{% extends 'base.html.twig' %}

{% block title %}Citas{% endblock %}

{% block body %}
    <h1>Citas</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ cita.id }}</td>
            </tr>
            <tr>
                <th>Fecha_creacion</th>
                <td>{{ cita.fechaCreacion ? cita.fechaCreacion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>Motivo</th>
                <td>{{ cita.motivo }}</td>
            </tr>
            <tr>
                <th>Estado</th>
                <td>{{ cita.estado }}</td>
            </tr>
{#             <tr>
                <th>PacienteId</th>
                <td>{{ cita.pacienteId }}</td>
            </tr>
            <tr>
                <th>MedicoId</th>
                <td>{{ cita.medicoId }}</td>
            </tr>
            <tr>
                <th>TipoCitaId</th>
                <td>{{ cita.tipoCitaId }}</td> #}
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_registro_citas_index') }}">back to list</a>

    <a href="{{ path('app_registro_citas_edit', {'id': cita.id}) }}">edit</a>

    {{ include('registro_citas/_delete_form.html.twig') }}
{% endblock %}
