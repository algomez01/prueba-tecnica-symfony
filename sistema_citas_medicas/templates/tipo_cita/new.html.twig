{% extends 'base.html.twig' %}

{% block title %}TipoCita{% endblock %}

{% block body %}
    

    <div style="border: 1px solid black; padding: 5px; margin: 5px; text-align: center;" id="header-div">
    <h1><center>Registre tipo de Cita</h1>
    <p>Sistema de citas médicas</p>
    {% if arrayMenu is defined %}
        {% for arrayMenuItem in arrayMenu %}
            <a href="{{ path(arrayMenuItem) }}">{{ arrayMenuItem }}</a>
        {% endfor %}
    {% endif %}
     {{ include('tipo_cita/_form.html.twig') }}

    <a href="{{ path('app_tipo_cita_index') }}">Ver el listado de citas</a>
    {# {{ include('tipo_cita/index.html.twig') }} #}
    </div>


    
{% endblock %}
