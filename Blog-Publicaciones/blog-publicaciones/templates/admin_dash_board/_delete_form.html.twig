<form method="post" action="{{ path('app_admin_dash_board_delete', {'id': user.id}) }}" onsubmit="return confirm('¿Estas seguro de querer borrar este usuario?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ user.id) }}">
    <button class="btn btn-danger">Eliminar</button>
</form>
