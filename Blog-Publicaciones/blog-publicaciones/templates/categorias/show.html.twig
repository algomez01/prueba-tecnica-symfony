{% extends 'base.html.twig' %}

{% block title %}Categorias{% endblock %}

{% block body %}
    <h1>Categorias</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ categoria.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ categoria.nomCategorias }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_categorias_index') }}">back to list</a>

    <a href="{{ path('app_categorias_edit', {'id': categoria.id}) }}"class="btn btn-primary" role="button">

    {{ include('categorias/_delete_form.html.twig') }}
{% endblock %}
