{% extends 'base.html.twig' %}

{% block title %}Comentarios{% endblock %}

{% block body %}
    <h1>Comentarios</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ comentario.id }}</td>
            </tr>
            <tr>
                <th>Body</th>
                <td>{{ comentario.body }}</td>
            </tr>
            <tr>
                <th>PublicacionesId</th>
                <td>{{ comentario.publicacionesId }}</td>
            </tr>
            <tr>
                <th>UserId</th>
                <td>{{ comentario.userId }}</td>
            </tr>
            <tr>
                <th>Fechacreacion</th>
                <td>{{ comentario.fechacreacion ? comentario.fechacreacion|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_usuario_comentarios_index') }}">back to list</a>

    <a href="{{ path('app_usuario_comentarios_edit', {'id': comentario.id}) }}">edit</a>

    {{ include('usuario_comentarios/_delete_form.html.twig') }}
{% endblock %}
